# docs/

Documentação e notas do repositório.

- `notes/` — documentos de análise, auditoria e anotações históricas.

> Observação: mantivemos na raiz os arquivos usados diretamente por código e/ou Docker (ex.: `docker-compose.yml`, `modelo_despesas_completo.csv`) para evitar quebrar fluxos existentes.
